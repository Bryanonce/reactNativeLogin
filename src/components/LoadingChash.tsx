import React from 'react'
import { Image } from 'react-native';

export const LoadingChash = () => {
    return(
        <Image 
            source={require('../resources/dolar.gif')}
        />

    );
}
